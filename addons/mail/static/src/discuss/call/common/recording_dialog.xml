<?xml version="1.0" encoding="UTF-8" ?>
<templates xml:space="preserve">

    <t t-name="discuss.RecordingDialog">
        <Dialog size="'sm'" footer="false" title.translate="Recording">
            <div class="o-discuss-RecordingDialog d-flex flex-column gap-1">
                <h3>Record the call</h3>
                <p class="mb-2">
                Recording will capture participants' audio, video, and screen shares. The recording file will be available in the chat once the call ends.
                </p>
                <div class="btn border-transparent d-flex gap-2" t-on-click="() => state.video = !state.video">
                    <div class="pe-2 d-flex align-items-center">
                        <input type="checkbox" role="switch" t-att-disabled="store.rtc?.recordingState.recording || !store.rtc?.canRecordVideo" t-att-checked="state.video ? 'checked' : ''"/>
                    </div>
                    <label class="d-flex align-items-center flex-wrap mw-100 cursor-pointer gap-2">
                        <span class="text-truncate text-wrap">Record video</span>
                    </label>
                </div>
                <div class="btn border-transparent d-flex gap-2" t-on-click="() => state.transcription = !state.transcription">
                    <div class="pe-2 d-flex align-items-center">
                        <input type="checkbox" role="switch" t-att-disabled="store.rtc?.recordingState.recording" t-att-checked="state.transcription ? 'checked' : ''"/>
                    </div>
                    <label class="d-flex align-items-center flex-wrap mw-100 cursor-pointer gap-2">
                        <span class="text-truncate text-wrap">Enable transcription</span>
                    </label>
                </div>
                <div class="d-flex justify-content-center p-2">
                <button class="btn btn-primary d-flex align-items-center justify-content-center gap-2 w-50" t-on-click="onClickRecording" t-att-disabled="store.rtc?.recordingRequest">
                    <span><t t-out="recordingButtonText"/></span>
                    <t t-if="store.rtc?.recordingRequest">
                        <i class="fa fa-fw fa-spinner fa-spin"/>
                    </t>
                </button>
                </div>
            </div>
        </Dialog>
    </t>

</templates>
